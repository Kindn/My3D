#include "Estimator.h"

#include <tuple>
#include <boost/filesystem.hpp>

// std::tuple<std::string, double, double> loadTimestamps(std::string const &data_dir);

int main(int argc, char **argv) {
  std::string const data_dir{argv[1]};

  // auto const timestamps{loadTimestamps(data_dir)};

  return 0;
}

